# 部署文档

### 1.1 安装 Node.js

访问 [Node.js 官网](https://nodejs.org/zh-cn) 下载 LTS 版本，安装时使用默认配置即可。安装包自带 npm 包管理器，无需单独安装。

验证安装：

```bash
node -v
npm -v
```

### 1.2 进入项目后，在项目根目录安装依赖

```bash
npm install
```

### 1.3 配置环境变量

复制 `.env.example` 文件内容，将以下内容复制，在根目录下创建.env.development，将刚刚复制的内容粘贴保存

或者也可以将.env.example的后缀删掉.example，变成.env保存

```bash
VITE_LOG_ISOPEN = "true"

# Vite 代理目标（后端服务器地址）
VITE_BASE_SERVER_PATH = "http://localhost:3140"
# 前端请求前缀（走代理的路径 ）
VITE_BASE_PROXY_SERVER_URL= "/api"

# 打包静态资源的前缀
VITE_BASE_H5_PROXY = ""
```

> 开发模式下，Vite 会根据 加载环境变量 自动配置代理，前端请求 `/api/xxx` 会被转发到 `http://localhost:3140/api/xxx`。

### 1.4 开发模式启动

```bash
npm run dev
```

启动后访问终端显示的 Local 地址（通常为 `http://localhost:5173`）